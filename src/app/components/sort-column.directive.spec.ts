import { SortColumnDirective } from './sort-column.directive';

describe('SortColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new SortColumnDirective();
    expect(directive).toBeTruthy();
  });
});
